{
  "name": "skyanalytics",
  "description": "SkyAnalytics is a service that allows you to track the behavior of your users.",
  "workspaces": [
    "packages/backend",
    "packages/frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@skyanalytics/frontend & npm run dev --workspace=@skyanalytics/backend"
  }
}
