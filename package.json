{
  "name": "skyanalytics",
  "description": "SkyAnalytics is a service that allows you to track the behavior of your users.",
  "workspaces": [
    "packages/backend",
    "packages/frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=@skyanalytics/backend",
    "dev:frontend": "npm run dev --workspace=@skyanalytics/frontend",
    "dev": "npm run dev:backend & npm run dev:frontend"
  }
}
